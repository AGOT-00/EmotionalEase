<!DOCTYPE html>
<html lang="en">
{{> head}}
  <body >
    {{> header}}
    <div class="bg-blue-200 min-h-screen flex items-center justify-center">
      
    <div class="bg-blue-200 h-screen flex justify-center items-center">
      <div class="bg-white p-8 rounded-lg shadow-lg">
        <h2 class="text-2xl font-semibold mb-4 text-center">Welcome back</h2>
        {{#if message}}
      <h1>{{message}}</h1>
    {{/if}}
        <form class="mb-4" method="post" action="/Login">
          <label class="block text-gray-700 font-semibold mb-2" for="email">
            Email Address
          </label>
          <input
            class="border border-gray-400 p-2 w-full rounded-lg"
            type="email"
            id="email"
            name="email"
            required
          />
          <label
            class="block text-gray-700 font-semibold mt-4 mb-2"
            for="password"
          >
            Password
          </label>
          <input
            class="border border-gray-400 p-2 w-full rounded-lg"
            type="password"
            id="password"
            name="password"
            required
          />
          <button
            class="bg-blue-500 text-white py-2 px-4 rounded-lg mt-4 w-full"
          >
            Sign In
          </button>
        </form>
        <p class="text-sm text-gray-600">
          Don't have an account?
          <a href="/Signup" class="text-blue-500 font-semibold">Sign up here.</a>
        </p>
        <p class="mt-4 text-gray-600">
          Need help? Click
          <a href="/ForgotPassword" class="text-blue-500 font-semibold">Here</a>.
        </p>
        <p class="mt-4 text-gray-600">
          Take a deep breath and remember that you've got this.
        </p>
      </div>
    </div>
    </div>
  </body>
</html>